name: First Workflow
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout Code
          uses: actions/checkout@v4
          with:
            node-version: 24
            cache: npm

        - name: Install Dependencies
          run: npm ci

        - name: react build
          run: npm run build
        
